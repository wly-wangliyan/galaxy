import { ZRegexDirective } from './z-regex.directive';

describe('ZRegexDirective', () => {
  it('should create an instance', () => {
    const directive = new ZRegexDirective();
    expect(directive).toBeTruthy();
  });
});
