import { UploadContentTypePipe } from './upload-content-type.pipe';

describe('UploadContentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new UploadContentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
