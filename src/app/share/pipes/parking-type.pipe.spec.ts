import { ParkingTypePipe } from './parking-type.pipe';

describe('ParkingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
