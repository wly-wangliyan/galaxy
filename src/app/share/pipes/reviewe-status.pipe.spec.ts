import { RevieweStatusPipe } from './reviewe-status.pipe';

describe('RevieweStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RevieweStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
