import { ParkingStatePipe } from './parking-state.pipe';

describe('ParkingStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingStatePipe();
    expect(pipe).toBeTruthy();
  });
});
