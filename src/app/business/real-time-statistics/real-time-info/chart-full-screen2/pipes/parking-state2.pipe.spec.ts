import { ParkingState2Pipe } from './parking-state2.pipe';

describe('ParkingState2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingState2Pipe();
    expect(pipe).toBeTruthy();
  });
});
