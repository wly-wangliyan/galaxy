import { ContentHoverDirective } from './content-hover.directive';

describe('ContentHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentHoverDirective();
    expect(directive).toBeTruthy();
  });
});
