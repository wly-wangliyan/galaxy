import { ShowFullScreenDblclickDirective } from './show-full-screen-dblclick.directive';

describe('ShowFullScreenDblclickDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFullScreenDblclickDirective();
    expect(directive).toBeTruthy();
  });
});
